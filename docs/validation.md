# 验证记录

普通任务直通与显式命令流程

`npm run check`：类型检查通过，123项测试全部通过。新增覆盖默认未启动时写入直通、`/design start` 后实施阻断、审查通过后执行、`/design stop` 恢复直通，以及重载/分支恢复后的许可失效。默认状态不会调用设计审查模型。


本轮 `npm run check` 通过：类型检查成功，122项测试全部通过。新增覆盖全文/起点/终点/显式范围、EOF实际范围、空文件、错误范围诊断、超过201行、超过1MB文件的小范围引用、UTF-8及累计预算边界。集成测试验证引用错误不会调用模型或进入reviewing，不丢弃此前产品问题，且撤销旧执行许可；成功结果返回实际引用范围。本轮未调用真实模型做端到端测试。

## 当前收缩版

`npm run check`：TypeScript 类型检查通过，115 个 node:test 用例全部通过，0 个跳过、0 个 TODO。`git diff --check` 通过。`git diff --check` 通过。`git diff --check` 通过。

本次离线回归覆盖：

- 非 Git 会话中提交方案、执行内建写工具及结束任务；只有一次 proposal 模型调用，不执行 Git，不做 operation 或 audit 审查。
- 超过 150KB 的已有暂存/未暂存文本及大型未跟踪二进制文件不进入模型载荷；真实索引、HEAD 和已有文件逐字节保持不变。
- 项目外的绝对路径、相对路径和符号链接可用于已审方案实施；实际读取项目外证据并执行内建 write。
- 伪造请求 ID、未审实施、同批审查写入、未知工具和未接入执行入口的外部写入/委派仍被阻断。
- 新输入、重提方案、重载、关闭会话使缓存许可失效；参数改写与重复消费许可不能执行。
- 待决问题不会被取消、单纯重提方案或 approve 命令抹掉。
- UI 回答保存并进入下一次审查；真实 interactive/rpc 文字回答与上一轮待决问题一同传入审查；两种回答均不直接产生实施许可。
- extension 注入消息、普通询问工具结果不形成批准；异步旧结果不能覆盖新请求。
- 恢复旧会话时保留请求、回答、待决审查，舍弃已删除的 dirty/基线数据。
- 可选 Git diff 查询仍支持空仓库和已提交仓库。
- 门禁中途启用前的用户原文和简短回答可以按当前分支entry ID引用；真实input捕获后的简短回答也可附带原始提问。
- 读取PI WEB已提交表单的结构化答案；取消、未回答和工具输出不构成授权。
- 最新输入仅为“继续”时携带最近上下文；显式历史引用保留之后的用户修正，其他分支及assistant自述不能作为request引用。
- 历史查询支持搜索和分页，不将整个历史转存到门禁状态；不复制无关的完整assistant会话。

## 对话式产品讨论

移除方案提交及回合结束时自动打开对话框的行为。离线集成测试覆盖：待决问题直接出现在会话中；取消、结束和重提不会重开；快捷入口的自由文本不进入已确认选项；用户可以多轮追问再明确选择，也可以讨论后返回手动选择。

运行 `scripts/eval-discussion.ts` 的 `/evaluate-discussion-gate`，使用 `yym/gpt-6-astra`：

| 用户行为 | 实际结果 |
| --- | --- |
| 追问“部分成功是什么意思，举个例子” | ask_user，要求先解释，不视为批准 |
| 假设“如果我选择接受部分成功……先解释，我还没决定” | ask_user，保留未决取舍 |
| 表示理解，但明确尚未决定 | ask_user，不从理解推导同意 |
| 讨论后明确接受部分成功和手动重试，要求按方案实现 | ready，不重复确认 |

4/4符合预期。验证只调用审查模型，不执行业务操作；弹窗行为由模拟UI测试验证，未做真实浏览器端到端测试。

## 历史授权遗漏的真实模型回归

运行 `scripts/eval-history.ts` 的 `/evaluate-history-gate`，使用当前配置的 `yym/gpt-6-astra`，从历史消息读取、证据解析和模型审查组成同一条验证路径：

| 场景 | 实际结果 |
| --- | --- |
| 启用前要求至少半分钟，用户对“30秒、测试后部署既定测试环境”回复“可以”，当前只说继续 | ready；明确复用历史授权，无需重复确认 |
| 部署同意来自启用前的PI WEB表单 | ready；认可真实已提交的结构化回答 |
| 历史同意部署，但之后明确取消部署、只改代码和测试 | revise；要求移除部署，保留修改和测试，不重复询问 |

3/3符合预期。本次不修改业务服务，也不执行任何真实部署。

测试使用注入的审查结果验证流程及执行拦截，不代表真实模型对所有必要性问题都判断正确。本次额外完成上述三项真实模型历史授权回归，未做浏览器或业务部署端到端测试。`scripts/smoke-live.mjs` 已调整为默认使用非 Git 目录；`--nested` 保留对子目录仓库的可选验证。

## 历史验证（旧版行为）

旧版曾在 Node.js 26.8.2、PI/pi-ai 0.84.4、Linux 上验证含逐次审查、Git 基线与自动终审的流程，并运行过 `scripts/smoke-live.mjs yym/gpt-6-astra` 及 `--nested`。这些结果不代表当前收缩版已完成真实模型端到端验证；相关工作区机制已经删除。

历史必要性模型评估的五个场景：局部 loading 修复得到 ready；无依据模板配置/缓存得到 revise；批量审核失败容忍度未知得到 ask_user；无旧数据却加迁移得到 revise；用户明确要求 Redis 补偿与重试得到 ready。样本仅说明当时这些例子符合预设，不能据此宣称泛化准确率。

当前保留 execute 包装器、epoch 失效与一次性许可，因为 PI 可能预检同批工具后才执行。已开始的副作用不能回滚；插件不再声称自动发现实施偏离方案。产品取舍与方案范围改变仍需主模型提交必要性依据。
