{
  "name": "pi-design-gate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PI extension enforcing evidence-based design review before implementation",
  "pi": { "extensions": ["./src/index.ts"] },
  "scripts": {
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": ">=0.84.4 <0.85",
    "@earendil-works/pi-coding-agent": ">=0.84.4 <0.85",
    "typebox": "^1.3.7"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  },
  "engines": { "node": ">=22.19" }
}
